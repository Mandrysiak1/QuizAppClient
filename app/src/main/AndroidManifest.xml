<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.quizzapp">

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme">

        <activity android:name=".activities.MenuActivity" />
        <activity android:name=".activities.GameActivity"/>
        <activity android:name=".activities.LobbyActivity"/>
        <activity android:name=".activities.ShopActivity"/>
        <activity android:name=".activities.SocietiesActivity"/>
        <activity android:name=".activities.QuestionsActivity"/>
        <activity android:name=".activities.AddQuestionActivity"/>
        <activity android:name=".activities.JoinSocietyActivity"/>
        <activity android:name=".activities.RankingActivity"/>
        <activity android:name=".activities.QuestActivity"/>
        <activity android:name=".activities.AuthenticationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>